// Supabase client removed — this project uses a Django REST backend.
// This stub prevents accidental imports of `@supabase/supabase-js`.

const supabase = null as unknown as Record<string, unknown> | null;

export { supabase };
export default supabase;
